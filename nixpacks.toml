# Use Python 3.10 (matching local)
providers = ["python"]

[phases.setup]
nixPkgs = ["python310", "ffmpeg"]

[phases.install]
cmds = ["pip install --upgrade pip", "pip install -r requirements.txt"]

[phases.build]
cmds = []

[start]
cmd = "uvicorn main3:app --host 0.0.0.0 --port ${PORT:-8000}"
